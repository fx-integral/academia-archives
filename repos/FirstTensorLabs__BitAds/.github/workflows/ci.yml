name: CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Test imports
      run: |
        # Test that the validator.py file has valid Python syntax and contains Validator class (without executing)
        python -c "import ast; content = open('neurons/validator.py').read(); tree = ast.parse(content); classes = [node.name for node in ast.walk(tree) if isinstance(node, ast.ClassDef)]; assert 'Validator' in classes, 'Validator class not found'; print('✅ Validator module syntax is valid'); print('✅ Validator class found in module')"

